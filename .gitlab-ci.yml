image: vincentcau/laravel-docker:latest

before_script:
  - composer install --prefer-dist --no-ansi --no-interaction --no-progress --no-scripts

unit-test-job:
  script:
    - php site/vendor/bin/phpunit --coverage-text --colors=never